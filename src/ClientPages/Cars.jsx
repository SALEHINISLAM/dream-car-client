import React from 'react';
import PropTypes from 'prop-types';

const Cars = props => {
    return (
        <div>
            cars
        </div>
    );
};

Cars.propTypes = {
    
};

export default Cars;